group: travis_latest
language: python
env:
    - REPO=inveniosoftware/cookiecutter-invenio-instance
    - REPO=inveniosoftware/dictdiffer  # has .travis.yml file
    - REPO=inveniosoftware/flask-celeryext  # has .travis.yml file
    - REPO=inveniosoftware/flask-iiif  # has .travis.yml file
    - REPO=inveniosoftware/flask-sitemap  # has .travis.yml file
    - REPO=inveniosoftware/idutils  # has .travis.yml file
    - REPO=inveniosoftware/invenio  # has .travis.yml file
    - REPO=inveniosoftware/invenio-access  # has .travis.yml file
    - REPO=inveniosoftware/invenio-accounts  # has .travis.yml file
    - REPO=inveniosoftware/invenio-admin  # has .travis.yml file
    - REPO=inveniosoftware/invenio-app  # has .travis.yml file
    - REPO=inveniosoftware/invenio-assets  # has .travis.yml file
    - REPO=inveniosoftware/invenio-base  # has .travis.yml file
    - REPO=inveniosoftware/invenio-cache  # has .travis.yml file
    - REPO=inveniosoftware/invenio-celery  # has .travis.yml file
    - REPO=inveniosoftware/invenio-config  # has .travis.yml file
    - REPO=inveniosoftware/invenio-db  # has .travis.yml file
    - REPO=inveniosoftware/invenio-files-rest  # has .travis.yml file
    - REPO=inveniosoftware/invenio-formatter  # has .travis.yml file
    - REPO=inveniosoftware/invenio-i18n  # has .travis.yml file
    - REPO=inveniosoftware/invenio-iiif  # has .travis.yml file
    - REPO=inveniosoftware/invenio-indexer  # has .travis.yml file
    - REPO=inveniosoftware/invenio-jsonschemas  # has .travis.yml file
    - REPO=inveniosoftware/invenio-mail  # has .travis.yml file
    - REPO=inveniosoftware/invenio-oaiserver  # has .travis.yml file
    - REPO=inveniosoftware/invenio-records-rest  # has .travis.yml file
    - REPO=inveniosoftware/invenio-search  # has .travis.yml file
    - REPO=inveniosoftware/invenio-userprofiles  # has .travis.yml file
    - REPO=inveniosoftware/invenio-webhooks  # has .travis.yml file
    - REPO=inveniosoftware/pytest-invenio  # has .travis.yml file
cache: pip
python:
    #- 2.7
    - 3.6
    #- nightly
    #- pypy
    #- pypy3
matrix:
    allow_failures:
        - python: nightly
        - python: pypy
        - python: pypy3
install:
    #- pip install -r requirements.txt
    - pip install flake8  # pytest  # add other testing frameworks later
before_script:
    - URL=https://github.com/${REPO}
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - git clone --depth=50 ${URL} ~/${REPO}  # --branch=master
    - cd ~/${REPO}
script:
    - echo stop the build if there are Python syntax errors or undefined names
    - echo ; echo -n "flake8 testing of ${URL} on " ; python -V
    - time flake8 . --count --select=E901,E999,F821,F822,F823 --show-source --statistics
    - echo exit-zero treats all errors as warnings.  The GitHub editor is 127 chars wide
    - time flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    #- true  # pytest --capture=sys
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
